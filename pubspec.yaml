name: use_case_dart
description: A pure-Dart use case result wrapper that funnels thrown errors into typed success/failure values. Zero dependencies.
version: 1.0.0
repository: https://github.com/adrian-antoci/use_case_dart

environment:
  sdk: ^3.11.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
